kind: Gateway
apiVersion: networking.x-k8s.io/v1alpha1
metadata:
  name: {{ .Values.gateway.name }}
spec:
  gatewayClassName: {{ .Values.gateway.class }}
  listeners:
{{ toYaml .Values.gateway.listeners | indent 4 }}
